name: CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    # ここから、実行するコマンドを指定する
    - name: Install dependencies
      run: npm ci
    - name: Build
      run: npm run build
